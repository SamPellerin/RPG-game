public class Main {
    // Main class, it calls the class ArgsProcessor with the specified arguments in the input.
    public static void main( String[] args ) {
        ArgsProcessor.process( args );
    }
}